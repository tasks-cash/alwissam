/**
 * Legacy Prisma clinic-user fixer — disabled.
 * Production path uses NestJS + MongoDB (`pnpm seed:owner`).
 */
console.error(
  "This legacy Prisma/PostgreSQL script is disabled. Use: pnpm seed:owner",
);
process.exit(1);
